function doGet(){return HtmlService.createTemplateFromFile("Consultation").evaluate().setTitle("Request a Consultation | Cost Control Consulting").setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL).addMetaTag("viewport","width=device-width, initial-scale=1")}function include(f){return HtmlService.createHtmlOutputFromFile(f).getContent()}function getPublicConfig(){return{timeZone:CCC.TIME_ZONE,timeZoneLabel:"Pacific Time",minimumBusinessDays:CCC.MIN_BUSINESS_DAYS,slotMinutes:CCC.SLOT_MINUTES}}
